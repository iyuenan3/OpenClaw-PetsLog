{
  "bsonType": "object",
  "required": ["petId", "type", "usedAt"],
  "permission": {
    "read": "auth.user_id in (SELECT _id FROM users WHERE familyId == (SELECT familyId FROM pets WHERE _id == doc.petId))",
    "create": "auth.user_id in (SELECT _id FROM users WHERE familyId == (SELECT familyId FROM pets WHERE _id == doc.petId))",
    "update": "auth.user_id in (SELECT _id FROM users WHERE familyId == (SELECT familyId FROM pets WHERE _id == doc.petId))",
    "delete": "auth.user_id in (SELECT _id FROM users WHERE familyId == (SELECT familyId FROM pets WHERE _id == doc.petId))"
  },
  "properties": {
    "_id": { "description": "ID，系统自动生成" },
    "petId": { "bsonType": "string", "description": "宠物 ID", "index": true },
    "type": { "bsonType": "string", "description": "类型", "enum": ["internal", "external"] },
    "brand": { "bsonType": "string", "description": "品牌" },
    "product": { "bsonType": "string", "description": "产品型号" },
    "usedAt": { "bsonType": "timestamp", "description": "使用日期" },
    "nextReminder": { "bsonType": "timestamp", "description": "下次提醒日期" },
    "remindEnabled": { "bsonType": "bool", "description": "是否启用提醒" }
  },
  "indexes": {
    "petId_index": {
      "keys": { "petId": 1 },
      "background": true
    },
    "petId_usedAt_index": {
      "keys": { "petId": 1, "usedAt": -1 },
      "background": true
    }
  }
}
